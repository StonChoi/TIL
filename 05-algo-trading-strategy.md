# 알고리즘 트레이딩 전략 설계

## 오늘 배운 것

- 이동평균선(Moving Average) 기반 골든크로스/데드크로스 전략의 기본 원리
  ```python
  df["MA20"] = df["Close"].rolling(20).mean()
  df["MA60"] = df["Close"].rolling(60).mean()
  ```
- RSI(상대강도지수)로 과매수/과매도 구간을 판단하는 법
- 매수/매도 시그널을 규칙(rule)으로 코드화하는 방법
- 전략을 세울 때 "왜 이 지표를 쓰는지" 가설을 먼저 세우는 게 중요하다는 것

## 느낀 점

전략 자체보다, 그 전략이 왜 통할 수 있는지에 대한 논리(가설)를 먼저 세우는 습관이 중요하다는 걸 배웠다. 지표를 아무리 많이 안다고 해도, 근거 없이 조합하면 과최적화(overfitting)로 이어질 수 있다는 점을 유의해야겠다.
