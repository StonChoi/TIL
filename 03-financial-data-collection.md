# 금융 데이터 수집 및 전처리

## 오늘 배운 것

- `yfinance` 라이브러리로 주가 데이터(OHLCV) 가져오는 법
  ```python
    import yfinance as yf
    df = yf.download("005930.KS", period="6mo")
    ```
- 결측치(NaN) 처리 — `dropna()`와 `fillna()`의 차이
- 날짜 인덱스를 기준으로 데이터를 정렬하고 리샘플링(resample)하는 법
- 이상치(outlier)를 박스플롯으로 시각적으로 확인하는 법

## 느낀 점

실제 금융 데이터는 생각보다 결측치와 이상치가 많다는 걸 처음 알았다. 전처리를 대충 하면 분석 결과 자체가 왜곡될 수 있어서, "데이터 클리닝"이 알고리즘만큼이나 중요한 작업이라는 걸 체감했다.
