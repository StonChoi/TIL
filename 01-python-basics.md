# Python 프로그래밍 기초

## 오늘 배운 것

- 변수, 자료형(int, float, str, bool)과 리스트/딕셔너리 기본 문법
- 리스트 컴프리헨션으로 반복문을 한 줄로 줄이는 법
  ```python
    squares = [x**2 for x in range(10)]
    ```
- 함수 정의와 기본 인자(default argument) 활용
- `if __name__ == "__main__":` 의 의미와 스크립트 실행 구조

## 느낀 점

기초 문법 자체보다, 코드를 "짧고 읽기 쉽게" 쓰는 습관이 나중에 데이터 분석 코드에서 훨씬 중요하다는 걸 느꼈다. 리스트 컴프리헨션처럼 파이썬다운(Pythonic) 표현을 익히는 데 집중해야겠다.
